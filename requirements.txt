# =============================================================================
# Find_Job_Pipe_Line_V2 - Python Dependencies
# Compatible with: Python 3.11, Spark 3.5.7, Airflow 2.8.0
# =============================================================================
# Install: pip install -r requirements.txt
# If Airflow dependency conflicts occur, install Airflow first with constraints:
#   pip install "apache-airflow[ssh]==2.8.0" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.8.0/constraints-3.11.txt"
#   pip install -r requirements.txt
# =============================================================================

# --- Core shared (aligned with Airflow 2.8.0 constraints, Spark 3.5.7 compatible) ---
numpy==1.24.4
pandas==2.1.4
pyarrow==11.0.0

# --- Apache Airflow 2.8.0 ---
apache-airflow==2.8.0
apache-airflow-providers-ssh==3.9.0

# --- PySpark 3.5.7 (match Spark cluster version) ---
pyspark==3.5.7

# --- Object storage & DB ---
minio>=7.2.0,<8
duckdb==0.9.2

# --- LLM / OpenAI ---
openai>=1.4.0,<2

# --- Web scraping & browser ---
selenium>=4.15.0
webdriver-manager>=4.0.0
requests>=2.31.0
beautifulsoup4==4.12.2

# --- Config & serialization ---
PyYAML==6.0.1
markdown==3.5.1

# --- ML / embeddings (Lib/Cluster_function.py, LLM) ---
scikit-learn>=1.3.0,<2
sentence-transformers>=2.2.0

# --- Airflow DB (when running Airflow locally) ---
psycopg2-binary==2.9.9

# --- Redundant but safe: transitive / common deps often needed ---
certifi>=2023.11.17
urllib3>=2.0.6,<3
charset-normalizer>=3.3.2
idna>=3.6
click>=8.1.7
Jinja2==3.1.2
Werkzeug>=2.2.3,<3
Flask>=2.2.5,<3
SQLAlchemy==1.4.50
cryptography>=41.0.7
paramiko>=3.3.1
bcrypt>=4.1.2
pyasn1>=0.5.1
typing_extensions>=4.9.0
packaging>=23.2
python-dateutil>=2.8.2
pytz>=2023.3
pytzdata==2020.1
tqdm>=4.66.1
filelock>=3.13.1
fsspec>=2023.12.2
lz4>=4.3.2
cloudpickle>=2.2.1
py4j>=0.10.9.7
